syntax = "proto3";
package new_window;

service NewWindowOpened {
    rpc OpenNewWindow (OpenNewWindowRequest) returns (OpenNewWindowResponse);
}

message OpenNewWindowRequest {}

message OpenNewWindowResponse {
    uint32 result = 1;
}
